# Data Folder

This folder stores sample and generated data used during pipeline execution.

CSV files are not tracked in Git because they are generated by the ingestion scripts.

Example pipeline:

BLS API → Python ingestion → CSV landing file → Snowflake stage → RAW table